-- Замысел сцены (этап 5 ТЗ индивидуальности, раздел 9.2).
--
-- Колонка, а не таблица: замысел живёт ровно одну генерацию, перегенерация
-- его заменяет, истории у него нет. Таблица со своими ревизиями обещала бы
-- то, чего этот слой не даёт.
--
-- NULL значит «подготовки не было»: старые главы, монолог без второго
-- участника, упавший вызов. Это отличается от пустого замысла, где модель
-- ответила, но участникам нечего хотеть.
ALTER TABLE `chapters` ADD COLUMN `scene_intent_json` text;
