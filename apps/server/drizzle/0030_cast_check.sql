-- Проверка различий состава (ТЗ 9.1, этап 5 слайс 4).
--
-- Колонка книги, а не поле в `studio_state`: отчёт описывает УТВЕРЖДЁННЫХ
-- героев (таблица `characters`), а не аспекты этапа, и запись через
-- `patchStudioState` наращивала бы ревизию Мастерской — открытая вкладка
-- получала бы 409 на следующей же правке раздела.
--
-- NULL значит «не проверяли». Отчёт несёт в себе состав на момент проверки
-- (герои и их ревизии), поэтому устаревание видно без отдельного поля.
ALTER TABLE `books` ADD COLUMN `cast_check_json` text;
